[project]
name = "eventflowsys"
version = "0.1.4"
description = "Base logger and internal event bus implementations."
authors = [ { name = "Your Name" } ]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "loguru>=0.7.3"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
]

[tool.uv]
# uv settings can be added here
