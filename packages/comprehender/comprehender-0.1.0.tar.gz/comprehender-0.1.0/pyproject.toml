[project]
name = "comprehender"
version = "0.1.0"
description = "Just a placeholder for the name \"comprehender\" in PyPI."
readme = "README.md"
authors = [
    { name = "Dmitry Tatarkin", email = "tatarkin@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.16,<0.9.0"]
build-backend = "uv_build"
