[project]
name = "mseep-k6-mcp-server"
version = "0.1.4"
description = "Model context protocol server for k6 load testing"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
]
authors = [
    { name = "mseep", email = "support@skydeck.ai" },
]

[project.readme]
content-type = "text/plain"
text = "Package managed by MseeP.ai"

[project.scripts]
mseep-k6-mcp-server = "main:main"

[tool.setuptools]
py-modules = [
    "main",
]
