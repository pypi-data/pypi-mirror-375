[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "aiko-monitor"
version = "0.0.5"
dependencies = ["httpx>=0.27.0,<1.0"]

[tool.setuptools.packages.find]
where = ["src2"]
