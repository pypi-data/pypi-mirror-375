[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chemevo"
version = "0.0.1"

dependencies = [
  "numpy",
  "astropy>=4",
  "scipy",
  "pandas",
  "matplotlib"
]