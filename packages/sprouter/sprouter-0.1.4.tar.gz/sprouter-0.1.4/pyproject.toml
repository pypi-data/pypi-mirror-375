[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sprouter"
version = "0.1.4"
description = "Simple Python project bootstrapper with virtualenv and pip automation."
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "</gadget_saavy>", email = "c.hirschauer@gmail.com" }
]
dependencies = []

[project.scripts]
sprouter = "sprouter.cli:main"

