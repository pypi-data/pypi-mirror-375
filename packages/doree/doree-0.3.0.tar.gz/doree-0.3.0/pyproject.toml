[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "doree"
version = "0.3.0"
description = "A sample Python package"
readme = "README.md"
authors = [{ name = "Capy Bara", email = "capyslayers@gmail.com" }]
license = { file = "LICENSE" }
dependencies = ["IPython"]  # List any dependencies

[tool.setuptools]
