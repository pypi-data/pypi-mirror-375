[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phiddipus"
version = "1.0.0"
description = "Asynchronous web vulnerability scanner"
readme = "README.md"
requires-python = ">=3.8"
authors = [
    { name="Spencer Harman" }
]
dependencies = [
    "httpx",
    "beautifulsoup4"
]

[project.scripts]
phiddipus = "phiddipus.cli:main"