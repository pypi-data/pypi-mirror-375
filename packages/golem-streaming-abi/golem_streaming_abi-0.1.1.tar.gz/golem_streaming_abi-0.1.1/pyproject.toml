[tool.poetry]
name = "golem-streaming-abi"
version = "0.1.1"
description = "Shared StreamPayment and ERC20 ABI definitions for VM on Golem"
authors = ["VM on Golem Contributors"]
packages = [
  { include = "golem_streaming_abi" }
]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

