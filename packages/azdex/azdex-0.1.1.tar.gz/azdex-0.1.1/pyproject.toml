[project]
name = "azdex"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "hhglory", email = "hhglory@outlook.com" }]
keywords = [
    "python dex libs",
    "dex",
    "Python Decentralized Crypto Exchange Libs",
]


requires-python = ">=3.12"
dependencies = ["hyperliquid-python-sdk>=0.18.0", "loguru>=0.7.3"]

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
