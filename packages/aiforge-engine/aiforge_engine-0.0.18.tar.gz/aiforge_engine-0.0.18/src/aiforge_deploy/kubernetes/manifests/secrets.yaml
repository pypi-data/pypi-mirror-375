apiVersion: v1
kind: Secret
metadata:
  name: aiforge-secrets
  namespace: aiforge
type: Opaque
data:
  # Base64 编码的 API 密钥
  # echo -n "your-openrouter-key" | base64
  openrouter-api-key: ""
  # echo -n "your-deepseek-key" | base64
  deepseek-api-key: ""
