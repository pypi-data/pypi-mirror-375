# Example environment for 00_simple_agent

# Required for OpenAI models (gpt-5, gpt-5-high)
LLM_OPENAI_API_KEY=

# Development mode (dev allows requests without API_TOKEN)
APP_ENV=dev

# Optional API token. If set, include Authorization header in requests
# API_TOKEN=changeme

# Optional: DATABASE_URL for persistence (dev mode continues without DB)
# DATABASE_URL=postgresql://user:pass@localhost:5432/pooolify
