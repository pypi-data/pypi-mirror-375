[build-system]
requires = ["setuptools>=61", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "oasis-api"
dynamic = ["version"]
description = "Python API for the Open Acquisition System for IEPE Sensors (OASIS)"
readme = "README.md"
authors = [
    {name = "Johannes Maierhofer", email = "j.maierhofer@tum.de"},
    {name = "Oliver Zobel", email = "oliver.zobel@tum.de"},
]
dependencies = [
    "pyserial",
    "numpy",
    "h5py",
    "matplotlib"
]
requires-python = ">=3.11"

[project.urls]
Homepage = "https://gitlab.com/oasis-acquisition/oasis-api"

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "node-and-date"
fallback_version = "0.0.0"
