[project]
name = "denario_app"
version = "0.1.4"
description = "GUI for Denario"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "streamlit-pdf-viewer>=0.0.23",
    "denario",
]
