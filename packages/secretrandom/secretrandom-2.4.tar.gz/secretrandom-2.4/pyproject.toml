[build-system]
requires = ['setuptools>=61.0']
build-backend = 'setuptools.build_meta'

[project]
name = 'secretrandom'
version = 'v2.4'
description = 'Unpredictable cryptographic random number & data generation (random + secrets module combined).'
readme = 'README.md'
requires-python = '>=3.11'
license = "MIT"
authors = [
  {name = 'dUhEnC-39', email = 'albeback01@gmail.com'}
]


[tool.setuptools.packages.find]
where = ['src']
