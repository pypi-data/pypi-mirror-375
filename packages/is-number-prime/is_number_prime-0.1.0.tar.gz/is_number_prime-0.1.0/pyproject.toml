[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "is_number_prime"
version = "0.1.0"
description = "Simple package for returning number is prime or not"
license = {text = "MIT"}
authors = [{name = "Your Name", email = "your.email@example.com"}]
requires-python = ">=3.6"


