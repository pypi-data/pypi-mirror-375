[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "german-snippets-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "polars>=1.33.0"
]

[project.scripts]
get-german-snippets = "german_snippets_data.main:main"

[tool.hatch.build.targets.wheel]
only-include = ["src/german_snippets_data"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
