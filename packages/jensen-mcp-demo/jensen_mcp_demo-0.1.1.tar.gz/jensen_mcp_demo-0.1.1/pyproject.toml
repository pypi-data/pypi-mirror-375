[project]
name = "jensen-mcp-demo"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jensen.chen", email = "jensen.chen@klook.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.1",
]

[project.scripts]
jensen-mcp-demo = "jensen_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
