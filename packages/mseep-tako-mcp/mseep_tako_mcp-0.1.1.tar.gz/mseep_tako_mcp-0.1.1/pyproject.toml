[project]
name = "mseep-tako-mcp"
version = "0.1.1"
description = "Tako MCP Server"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]==1.12.3",
    "tako-sdk>=0.1.34",
]
authors = [
    { name = "mseep", email = "support@skydeck.ai" },
]

[project.readme]
content-type = "text/plain"
text = "Package managed by MseeP.ai"

[project.scripts]
mseep-tako-mcp = "main:main"

[tool.setuptools]
py-modules = [
    "main",
]
