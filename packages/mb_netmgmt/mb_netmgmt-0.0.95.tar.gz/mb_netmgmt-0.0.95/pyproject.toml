[build-system]
requires = ["flit"]
build-backend = "flit_core.buildapi"

[project]
name = "mb_netmgmt"
version = "0.0.95"
dynamic = ["description"]
dependencies = [
    "ncclient",
    "paramiko>=2.12.0",
    "ruamel.yaml",
    "requests",
    "scapy>=2.6.0",
    "exscript",
]

[tool.uv]
dev-dependencies = ["pytest", "flit"]
