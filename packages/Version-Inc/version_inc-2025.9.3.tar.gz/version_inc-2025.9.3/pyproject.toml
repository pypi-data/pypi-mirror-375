[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Version-Inc"
version = "2025.9.3" # vinc
authors = [
  { name="Günthner" },
]
description = "A lightweight tool for incrementing version numbers in files"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Environment :: Console"
]
# dependencies = []
keywords=["Versions"]

[project.scripts]
vinc = "version_inc:command_entry_point"