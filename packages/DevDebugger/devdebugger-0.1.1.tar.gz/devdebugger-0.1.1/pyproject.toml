[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "DevDebugger"
version = "0.1.1"
description = "A debugging and profiling helper library for developers"
authors = [
    { name="4rmy", email="hjacob215@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
