[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wolflive"
version = "0.0.1"
description = "WOLFApi-TS ported to Python (async-first)."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = ["aiohttp>=3.8", "websocket-client>=1.6", "requests>=2.0"]
authors = [{name = "Converted by", email = "noreply@example.com"}]
