[project]
name = "autotui"
version = "0.4.8"
description = "\"quickly create UIs to interactively prompt, validate, and persist python objects to disk (JSON/YAML) and back using type hints\""
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
keywords = ["data", "prompt", "namedtuple"]
classifiers = [
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "PyYAML>=6.0",
  "click>=8.0",
  "dateparser>=1.1.0",
  "prompt-toolkit>=3.0",
]
[[project.authors]]
name = "purarue"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.urls]
Homepage = "https://github.com/purarue/autotui"

[project.optional-dependencies]
edit = ["click"]
json = ["orjson"]
optional = ["click", "orjson", "pyfzf-iter"]
pick = ["pyfzf-iter"]
testing = ["flake8", "mypy", "pytest"]

[tool.mypy]
pretty = true
disallow_any_generics = false
show_error_context = true
show_error_codes = true
namespace_packages = true
disallow_subclassing_any = true
disallow_incomplete_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true

[tool.pytest.ini_options]
addopts = "--doctest-modules autotui tests"

[tool.setuptools.packages.find]
include = ["autotui"]
exclude = ["tests*"]
