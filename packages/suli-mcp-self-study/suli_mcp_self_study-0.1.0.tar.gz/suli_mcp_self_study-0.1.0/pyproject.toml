[project]
name = "suli-mcp-self-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "xuanyun-su", email = "suli249221@163.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.1",
]

[project.scripts]
suli-mcp-self-study = "suli_mcp_self_study:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
