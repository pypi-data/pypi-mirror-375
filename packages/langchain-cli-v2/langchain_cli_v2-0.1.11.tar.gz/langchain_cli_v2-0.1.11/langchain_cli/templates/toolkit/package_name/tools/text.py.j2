"""Text tools for {{ toolkit_name }}."""

from langchain_tool_server import tool


@tool
def reverse(text: str) -> str:
    """Reverse a string."""
    return text[::-1]


@tool
def uppercase(text: str) -> str:
    """Convert text to uppercase."""
    return text.upper()