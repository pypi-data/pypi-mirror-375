#!/bin/bash
## convert between different Japanese encodings (Network Kanji Filter, nkf)
#################################
## NOTE #########################
## nkf -w → UTF-8
## nkf -s → Shift-JIS
## nkf -e → EUC-JP
## nkf -g → guess encoding
## -Lu → convert line endings to UNIX (\n)
## -Lw → convert line endings to Windows (\r\n)
## -Lm → convert line endings to Mac (\r)
#################################
## ARGUMENTS ####################
## input
## output
## SCRIPT #####################

nkf -w ${input} > ${output}