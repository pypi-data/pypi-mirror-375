[project]
name = "quarktorch"
version = "0.0.1"
description = "The smallest torch in the ML universe."
authors = [
    {name = "SyJarvis",email = "1755115828@qq.com"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]

[tool.poetry]
packages = [{include = "quarktorch", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
