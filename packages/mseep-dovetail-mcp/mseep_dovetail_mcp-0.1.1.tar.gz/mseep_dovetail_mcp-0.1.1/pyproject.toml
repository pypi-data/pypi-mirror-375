[project]
name = "mseep-dovetail-mcp"
version = "0.1.1"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.9.2",
]
authors = [
    { name = "mseep", email = "support@skydeck.ai" },
]

[project.readme]
content-type = "text/plain"
text = "Package managed by MseeP.ai"

[project.scripts]
mseep-dovetail-mcp = "main:main"

[dependency-groups]
dev = [
    "ruff>=0.11.12",
]

[tool.setuptools]
py-modules = [
    "main",
]
