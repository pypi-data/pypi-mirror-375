[project]
name = "rdpairs"
version = "0.1.0"
description = ""
authors = [
    {name = "DeYing Zhang",email = "legendzdy@dingtalk.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer (>=0.15.1,<0.16.0)",
    "basebio (>=0.4.8)"
]

[tool.poetry.scripts]
rdpairs = "rdpairs.main:app"

[[tool.poetry.source]]
name = "pypi2"
url = "https://pypi.tuna.tsinghua.edu.cn/simple/"
priority = "primary"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"

