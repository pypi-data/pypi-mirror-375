[project]
name = "mseep-cbi-mcp-server"
version = "0.1.1"
description = "MCP Server for interacting with ChatCBI"
requires-python = ">=3.12"
dependencies = [
    "httpx==0.28.1",
    "mcp[cli]==1.8.1",
    "python-dotenv==1.1.0",
]
authors = [
    { name = "mseep", email = "support@skydeck.ai" },
]

[project.readme]
content-type = "text/plain"
text = "Package managed by MseeP.ai"

[project.scripts]
mseep-cbi-mcp-server = "server:main"

[tool.setuptools]
py-modules = [
    "server",
]
