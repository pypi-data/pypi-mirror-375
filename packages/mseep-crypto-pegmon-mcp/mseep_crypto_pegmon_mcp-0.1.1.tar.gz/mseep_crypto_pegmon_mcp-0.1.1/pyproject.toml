[project]
name = "mseep-crypto-pegmon-mcp"
version = "0.1.1"
description = "An MCP server that tracks stablecoin peg integrity across multiple blockchains"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.7.1",
    "pandas>=2.2.3",
    "pycoingecko>=3.2.0",
    "tabulate>=0.9.0",
]
authors = [
    { name = "mseep", email = "support@skydeck.ai" },
]

[project.readme]
content-type = "text/plain"
text = "Package managed by MseeP.ai"

[project.scripts]
mseep-crypto-pegmon-mcp = "main:main"

[tool.setuptools]
py-modules = [
    "main",
]
