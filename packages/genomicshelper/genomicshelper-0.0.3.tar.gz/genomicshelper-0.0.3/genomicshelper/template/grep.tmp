#!/bin/bash
## grep command with commonly used options
#################################
## NOTE #########################
## -E   Use extended regular expressions (ERE), same as egrep
## -F 	Interpret pattern as fixed string (no regex), same as fgrep
## -H   Always print filename before matches (default if searching multiple files).
## -A   Print NUM lines after each match.
## -B   Print NUM lines before each match.
## -C   Print NUM lines before and after matches (context).
## -v   Invert match (select non-matching lines).
## -i 	Ignore case (case-insensitive search).
## -n   Show line numbers.
#################################
## ARGUMENTS ####################
## pat: pattern
## file : path to search
## flag: options
## SCRIPT #####################

grep ${flag} "${pat}" ${file}
