"""
Main entry point for agentmap.core.cli package.

This allows the package to be executed directly as:
python -m agentmap.core.cli
"""

from agentmap.core.cli.main_cli import main_cli

if __name__ == "__main__":
    main_cli()
