[project]
name = "hts-tst-mcp-demo"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tianshi", email = "tianshi.huang@soucaigroup.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.12.2",
    "pandas>=2.3.2",
]

[project.scripts]
hts-tst-mcp-demo = "hts_tst_mcp_demo:main"

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"
