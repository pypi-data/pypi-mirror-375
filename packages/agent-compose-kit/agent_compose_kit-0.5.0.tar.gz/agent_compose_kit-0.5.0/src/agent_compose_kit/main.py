def main() -> None:
    """Library entrypoint placeholder.

    This package is intended for programmatic use. Import functions from:
    - src.config.models (load_config_file, AppConfig, ...)
    - src.services.factory (build_session_service, build_artifact_service, ...)
    - src.agents.builder (build_agents)
    - src.runtime.supervisor (build_plan, build_runner_from_yaml, build_run_config)
    """
    # No CLI. Keeping a no-op main to preserve module entry point if invoked.
    return None


if __name__ == "__main__":
    main()
